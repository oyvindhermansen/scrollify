# scrollzy

## Install

```
$ git clone https://github.com/oyvindhermansen/scrollify.git && cd scrollzy
$ npm install
```

## Usage

```javascript
import scrollzy from 'scrollzy'

scrollzy({
  selector: $('#yourButton'),
  scrollTo: $('#scrollHere'),
  speed: 1000 // default
})
```
